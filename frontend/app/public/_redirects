# Cloudflare Pages already serves the static entity pages (edgeproc.html,
# github.html, faq.html) at their CLEAN URLs (/edgeproc, /github, /faq)
# automatically — and 308-redirects the .html form to the clean form.
# Do NOT add an explicit `/edgeproc -> /edgeproc.html 200` rewrite: it fights
# that built-in clean-URL redirect and produces an infinite /edgeproc loop.
# (Static assets are served before this file, so the entity pages resolve
# without any rule here.)

# SPA fallback — keep LAST. Any route without a static file renders the app shell.
/*    /index.html   200
