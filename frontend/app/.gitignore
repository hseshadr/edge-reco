# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Playwright
test-results
playwright-report
playwright/.cache

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Self-hosted model weights — materialized by scripts/download-model.mjs (the
# `prebuild` hook), sha256-pinned, never committed (no tracked binaries >1MB).
public/models/

# Self-hosted onnxruntime-web wasm runtime — staged from node_modules by
# scripts/stage-ort-wasm.mjs (the `prebuild` hook); never committed.
public/ort/
