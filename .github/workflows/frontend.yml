name: Frontend CI

# The Nimbus storefront + the extracted @edgeproc/browser package live in an npm
# workspace rooted at frontend/. The existing ci.yml covers only the Python core;
# this workflow closes the long-standing gap where the frontend gate was
# local-only. It mirrors the verified-green local hard gate exactly:
# biome -> tsc -> vitest -> production build, across both workspace members.
# Playwright e2e now also runs in CI (the `e2e` job below): both the storefront
# suite and the C1 sync + in-browser embed model-load smoke suite, each starting
# its own Vite + static-catalog webServer. The HuggingFace MiniLM model download
# is cached so the ~25MB weights are not re-fetched on every run.
#
# All run: steps use static commands only — no untrusted ${{ github.event.* }}
# input is interpolated anywhere, so there is no workflow-injection surface.

on:
  push:
    branches: [main]
  pull_request:

jobs:
  frontend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v4
        with:
          node-version: "24" # >= 22.12 required by Vite 8
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install workspace
        run: npm ci

      - name: Lint (biome) — package + app
        run: npm run lint --workspaces --if-present

      - name: Typecheck (tsc) — package + app
        run: npm run typecheck --workspaces --if-present

      - name: Unit tests (vitest) — package + app
        run: npm run test --workspaces --if-present

      - name: Build app (proves the workspace link resolves)
        run: npm run build -w frontend

  e2e:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    # Both Playwright suites start their own Vite + static-catalog webServers.
    # The C1 suite includes an in-browser embed smoke test that loads the real
    # ~25MB Xenova/all-MiniLM-L6-v2 weights from the HuggingFace CDN; HF_HOME and
    # TRANSFORMERS_CACHE are pinned to a cached dir so the weights download once
    # and are restored on subsequent runs instead of being re-fetched.
    env:
      HF_HOME: ${{ github.workspace }}/.cache/huggingface
      TRANSFORMERS_CACHE: ${{ github.workspace }}/.cache/huggingface
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v4
        with:
          node-version: "24" # >= 22.12 required by Vite 8
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install workspace
        run: npm ci

      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-${{ runner.os }}-${{ hashFiles('frontend/package-lock.json') }}

      - name: Install Playwright Chromium + system deps
        working-directory: frontend/app
        run: npx playwright install --with-deps chromium

      - name: Cache HuggingFace model weights
        uses: actions/cache@v4
        with:
          path: ${{ github.workspace }}/.cache/huggingface
          # Keyed on the model id so the ~25MB MiniLM weights survive across runs
          # and are only re-downloaded if the pinned model changes.
          key: hf-model-${{ runner.os }}-Xenova-all-MiniLM-L6-v2

      - name: E2E — storefront suite (default config)
        working-directory: frontend/app
        run: npx playwright test

      - name: E2E — C1 sync + embed model-load smoke suite
        working-directory: frontend/app
        run: npx playwright test -c playwright.c1.config.ts

      - name: Upload Playwright report + traces on failure
        if: ${{ failure() }}
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: |
            frontend/app/playwright-report
            frontend/app/test-results
          if-no-files-found: ignore
          retention-days: 7
